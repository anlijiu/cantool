name: can
description: can plugin.
version: 0.0.2
author: anlijiu <anlijiu@gmail.com>
homepage: https://github.com/anlijiu/cantool

flutter:
  plugin:
    platforms:
      linux:
        pluginClass: CanPlugin

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  json_annotation: ^4.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^1.10.2
  json_serializable: ^4.0.1
  freezed: ^0.14.1+2
