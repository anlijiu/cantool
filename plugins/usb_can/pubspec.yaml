name: usb_can
description: usb can plugin.
version: 0.0.1
author: anlijiu <anlijiu@gmail.com>
homepage: https://github.com/anlijiu/cantool

flutter:
  plugin:
    pluginClass: UsbCan 

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  json_annotation: ^2.4.0
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^1.0.0
  json_serializable: ^3.0.0
